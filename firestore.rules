rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to authenticated users (development mode)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
    // Production rules (commented out for now)
    // Uncomment and customize these rules for production deployment
    
    // match /users/{userId} {
    //   allow read: if request.auth != null;
    //   allow write: if request.auth.uid == userId || 
    //                   get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role in ['Admin', 'Ketua LPM'];
    // }
    
    // match /indicators/{indicatorId} {
    //   allow read: if request.auth != null;
    //   allow write: if get(/databases/$(database)/documents/users/$(request.auth.uid)).data.permissions has 'manage_indicators';
    // }
    
    // match /monev_sessions/{sessionId} {
    //   allow read: if request.auth != null;
    //   allow write: if get(/databases/$(database)/documents/users/$(request.auth.uid)).data.permissions has 'manage_monev';
    // }
    
    // match /settings/{settingId} {
    //   allow read: if request.auth != null;
    //   allow write: if get(/databases/$(database)/documents/users/$(request.auth.uid)).data.permissions has 'manage_settings';
    // }
  }
}
